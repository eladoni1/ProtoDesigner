PD0074 bus 'Shared' / message 'Alpha' / field 'header.messageId': Field 'header.messageId' is 1 byte(s). Protobuf's integers are 32- or 64-bit, so this would go on the wire as a different size than you designed. Set its wire size to 4 or 8 bytes, or keep this message on the C target.
PD0074 bus 'Shared' / message 'Alpha' / field 'value': Field 'value' is 2 byte(s). Protobuf's integers are 32- or 64-bit, so this would go on the wire as a different size than you designed. Set its wire size to 4 or 8 bytes, or keep this message on the C target.
PD0074 bus 'Shared' / message 'Beta' / field 'envelope.head.messageId': Field 'envelope.head.messageId' is 1 byte(s). Protobuf's integers are 32- or 64-bit, so this would go on the wire as a different size than you designed. Set its wire size to 4 or 8 bytes, or keep this message on the C target.
PD0074 bus 'Shared' / message 'Beta' / field 'envelope.sequence': Field 'envelope.sequence' is 2 byte(s). Protobuf's integers are 32- or 64-bit, so this would go on the wire as a different size than you designed. Set its wire size to 4 or 8 bytes, or keep this message on the C target.
PD0074 bus 'Shared' / message 'Beta' / field 'header.messageId': Field 'header.messageId' is 1 byte(s). Protobuf's integers are 32- or 64-bit, so this would go on the wire as a different size than you designed. Set its wire size to 4 or 8 bytes, or keep this message on the C target.
