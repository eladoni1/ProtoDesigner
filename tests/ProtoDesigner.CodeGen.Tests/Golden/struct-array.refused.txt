PD0074 bus 'Sensors' / message 'Bundle' / field 'blockCount': Field 'blockCount' is 1 byte(s). Protobuf's integers are 32- or 64-bit, so this would go on the wire as a different size than you designed. Set its wire size to 4 or 8 bytes, or keep this message on the C target.
PD0074 bus 'Sensors' / message 'Bundle' / field 'blocks[].samples[]': Field 'blocks[].samples[]' is 1 byte(s). Protobuf's integers are 32- or 64-bit, so this would go on the wire as a different size than you designed. Set its wire size to 4 or 8 bytes, or keep this message on the C target.
PD0074 bus 'Sensors' / message 'Bundle' / field 'readingCount': Field 'readingCount' is 1 byte(s). Protobuf's integers are 32- or 64-bit, so this would go on the wire as a different size than you designed. Set its wire size to 4 or 8 bytes, or keep this message on the C target.
PD0074 bus 'Sensors' / message 'Bundle' / field 'readings[].channel': Field 'readings[].channel' is 1 byte(s). Protobuf's integers are 32- or 64-bit, so this would go on the wire as a different size than you designed. Set its wire size to 4 or 8 bytes, or keep this message on the C target.
PD0074 bus 'Sensors' / message 'Bundle' / field 'readings[].value': Field 'readings[].value' is 1 byte(s). Protobuf's integers are 32- or 64-bit, so this would go on the wire as a different size than you designed. Set its wire size to 4 or 8 bytes, or keep this message on the C target.
