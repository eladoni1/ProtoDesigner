<Window x:Class="ProtoDesigner.Wpf.Views.Dialogs.TextPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title, RelativeSource={RelativeSource Self}}"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
  <Border Background="{StaticResource Surface1}" Padding="20">
    <StackPanel>
      <TextBlock x:Name="PromptText" Style="{StaticResource SectionLabel}" Margin="0,0,0,6"/>
      <TextBox x:Name="Input" FontSize="{StaticResource TextLg}" Padding="10,7"
               KeyDown="OnKeyDown"/>
      <TextBlock x:Name="HintText" Style="{StaticResource CaptionText}" Margin="0,8,0,0"
                 TextWrapping="Wrap" Visibility="Collapsed"/>
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
        <Button Content="Cancel" Style="{StaticResource GhostButton}" Click="OnCancel" MinWidth="88"/>
        <Button Content="OK" Style="{StaticResource PrimaryButton}" Click="OnOk" MinWidth="88" Margin="8,0,0,0"/>
      </StackPanel>
    </StackPanel>
  </Border>
</Window>
