<UserControl x:Class="ProtoDesigner.Wpf.Views.LayoutListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Style="{StaticResource SectionHeaderBar}">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
          <TextBlock Style="{StaticResource SectionLabel}" Text="Wire layout"/>
          <TextBlock Text="{Binding Title}" Style="{StaticResource H3Text}"/>
        </StackPanel>
        <Border Grid.Column="1" Style="{StaticResource Chip}" Background="{StaticResource Surface3}">
          <TextBlock Text="{Binding Summary}" FontFamily="{StaticResource MonoFont}" FontSize="11"
                     Foreground="{StaticResource Ink}"/>
        </Border>
      </Grid>
    </Border>

    <!-- Column headers -->
    <Border Grid.Row="1" Background="{StaticResource Surface0}"
            BorderBrush="{StaticResource SurfaceStroke}" BorderThickness="0,0,0,1" Padding="16,6"
            Visibility="{Binding HasRows, Converter={StaticResource BoolToVis}}">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="52"/>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="72"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Style="{StaticResource SectionLabel}" Text="Byte" ToolTip="Byte offset from the start of the message"/>
        <TextBlock Grid.Column="1" Style="{StaticResource SectionLabel}" Text="Field"/>
        <TextBlock Grid.Column="2" Style="{StaticResource SectionLabel}" Text="Size" TextAlignment="Right"/>
      </Grid>
    </Border>

    <Grid Grid.Row="2">
      <TextBlock Style="{StaticResource CaptionText}" TextAlignment="Center" Margin="20,30"
                 TextWrapping="Wrap" Text="{Binding Summary}"
                 Visibility="{Binding HasRows, Converter={StaticResource BoolToVis}, ConverterParameter=invert}"/>

      <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="8,6"
                    Visibility="{Binding HasRows, Converter={StaticResource BoolToVis}}">
        <ItemsControl ItemsSource="{Binding Rows}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Border Padding="8,6" Margin="0,1" CornerRadius="4" Background="{StaticResource Surface2}">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="52"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="72"/>
                  </Grid.ColumnDefinitions>

                  <TextBlock Grid.Column="0" Text="{Binding Offset}"
                             FontFamily="{StaticResource MonoFont}" FontSize="11"
                             Foreground="{StaticResource InkDim}" VerticalAlignment="Center"/>

                  <StackPanel Grid.Column="1" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                      <TextBlock Text="{Binding TypeName}" FontFamily="{StaticResource MonoFont}"
                                 FontSize="12" Foreground="{StaticResource InkMuted}"/>
                      <TextBlock Text="{Binding Path}" FontFamily="{StaticResource MonoFont}"
                                 FontSize="12" Foreground="{StaticResource Ink}" Margin="6,0,0,0"
                                 TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                    <TextBlock Text="{Binding Detail}" Style="{StaticResource CaptionText}" FontSize="11"
                               Visibility="{Binding Detail, Converter={StaticResource EmptyToVis}}"/>
                  </StackPanel>

                  <TextBlock Grid.Column="2" Text="{Binding Size}" TextAlignment="Right"
                             FontFamily="{StaticResource MonoFont}" FontSize="12"
                             Foreground="{StaticResource Ink}" VerticalAlignment="Center"/>
                </Grid>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>
    </Grid>
  </Grid>
</UserControl>
